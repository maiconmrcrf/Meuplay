services:
  - type: web
    name: marcostvmrc
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn -b 0.0.0.0:$PORT universaltv:app
    plan: free
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
